\documentclass[conference]{IEEEtran}
\IEEEoverridecommandlockouts

% --- CODIFICAÇÃO ---
\usepackage[T1]{fontenc}

% --- IDIOMA ---
$if(lang)$
\usepackage[$lang$]{babel}
$else$
\usepackage[english]{babel}
$endif$

% -------------------------------------------------------------------
% FONTE  —  controlada por flags booleanas geradas pelo build.sh
%
%   font-arial     → Liberation Sans     (métricas idênticas à Arial)
%   font-helvetica → Nimbus Sans         (Helvetica livre, URW)
%   font-times     → TeX Gyre Termes     (clone livre do Times New Roman)
%   font-ubuntu    → Ubuntu              (família clássica, estáticas disponíveis)
%
% Padrão (nenhuma flag): TeX Gyre Termes (IEEE recomenda Times New Roman)
% -------------------------------------------------------------------
\usepackage{fontspec}
$if(font-arial)$
  \setmainfont{Liberation Sans}
$else$$if(font-helvetica)$
  \setmainfont{Nimbus Sans}
$else$$if(font-ubuntu)$
  \setmainfont{Ubuntu}
$else$
  \setmainfont{TeX Gyre Termes}
$endif$$endif$$endif$

% --- PACOTES IEEE PADRÃO ---
\usepackage{cite}
\usepackage{amsmath, amssymb, amsfonts}
\usepackage{graphicx}
\usepackage{textcomp}
\usepackage{xcolor}
\usepackage{booktabs}
\usepackage{longtable}
\usepackage{hyperref}
\usepackage{array}
\usepackage{csquotes}
\usepackage{microtype}
% --- IMAGE ASPECT RATIO ---
% Ensures that specifying only width or only height in Markdown image
% attributes scales the missing dimension proportionally. When both are
% given they act as maximum bounds, preventing distortion in all cases.
\makeatletter
\let\Oldincludegraphics\includegraphics
\renewcommand{\includegraphics}[2][]{\Oldincludegraphics[keepaspectratio,#1]{#2}}
\makeatother

% --- SUPORTE A CÓDIGO (Pandoc) ---
\usepackage{fancyvrb}
\usepackage{listings}
\usepackage{upquote}
\newcommand{\passthrough}[1]{#1}
\DefineVerbatimEnvironment{Highlighting}{Verbatim}{commandchars=\\\{\}}
\newenvironment{Shaded}{}{}
\newcommand{\KeywordTok}[1]{\textcolor{blue}{\textbf{#1}}}
\newcommand{\StringTok}[1]{\textcolor{teal}{#1}}
\newcommand{\CommentTok}[1]{\textcolor{gray}{\textit{#1}}}
\newcommand{\FunctionTok}[1]{\textcolor{cyan}{#1}}
\newcommand{\NormalTok}[1]{#1}
\newcommand{\OtherTok}[1]{#1}
\newcommand{\ErrorTok}[1]{\textcolor{red}{#1}}
\newcommand{\DataTypeTok}[1]{\textcolor{purple}{#1}}
\newcommand{\DecValTok}[1]{\textcolor{red}{#1}}
\newcommand{\ControlFlowTok}[1]{\textcolor{blue}{\textbf{#1}}}
\newcommand{\BuiltInTok}[1]{\textcolor{purple}{#1}}
\newcommand{\ImportTok}[1]{\textcolor{teal}{#1}}
\newcommand{\AttributeTok}[1]{\textcolor{orange}{#1}}
\newcommand{\OperatorTok}[1]{#1}
\newcommand{\ExtensionTok}[1]{#1}

\def\BibTeX{{\rm B\kern-.05em{\sc i\kern-.025em b}\kern-.08em
    T\kern-.1667em\lower.7ex\hbox{E}\kern-.125emX}}

\begin{document}

\title{$title$$if(thanks)$\thanks{$thanks$}$endif$}

\author{
$for(author)$
  \IEEEauthorblockN{$if(author.name)$$author.name$$else$$author$$endif$}
  $if(author.institution)$
  \IEEEauthorblockA{
    \textit{$author.institution$}\\
    $if(author.city)$$author.city$$if(author.country)$, $author.country$$endif$\\$endif$
    $if(author.email)$$author.email$$endif$
  }
  $endif$
  $sep$\and
$endfor$
}

\maketitle

$if(abstract-en)$
\begin{abstract}
$abstract-en$
\end{abstract}
$endif$

$if(abstract-pt)$
\begin{abstract}
$abstract-pt$
\end{abstract}
$endif$

$if(keywords-en)$
\begin{IEEEkeywords}
$keywords-en$
\end{IEEEkeywords}
$endif$

$if(keywords-pt)$
\begin{IEEEkeywords}
$keywords-pt$
\end{IEEEkeywords}
$endif$

$body$

$if(bibliography)$
\bibliographystyle{IEEEtran}
\bibliography{$bibliography$}
$endif$

\end{document}
